#include <stdio.h>
#include <math.h>

float calculateX(int i, int j, int k)
{
    return j * sin(A) * sin(B) * cos(C) - k * cos(A) * sin(B) * cos(C) +
           j * cos(A) * sin(C) + k * sin(A) * sin(C) + i * cons(B) * cos(C);
}

int main (int argc, char *argv)
{
    return 0;
}