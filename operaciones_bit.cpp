#include <iostream>
#include <bitset>

using namespace std;

int main(){
    int arr[7]= {0, 0, 0, 0, 0, 0, 0};
    arr[1] &= ~(1);
    unsigned short  short1 = 4;
    bitset<16>

    for(int i=0; i<=7; i++ ){
        std::cout<<arr[i];
        std::cout<<std::endl;
    }

    return 0;
}